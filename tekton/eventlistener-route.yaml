apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: el-plc-waveform-listener
  labels:
    app: plc-waveform
spec:
  to:
    kind: Service
    name: el-plc-waveform-listener
  port:
    targetPort: http-listener
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
